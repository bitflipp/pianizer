{
  "name": "pianizer",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "vitest run && NODE_OPTIONS=--no-deprecation playwright test",
    "test:engine": "vitest run",
    "test:watch":    "vitest",
    "test:coverage": "vitest run --coverage",
    "test:ui":       "NODE_OPTIONS=--no-deprecation playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.0",
    "@vitest/coverage-v8": "^4.1.7",
    "happy-dom": "^20.9.0",
    "vitest": "^4.1.7"
  }
}
